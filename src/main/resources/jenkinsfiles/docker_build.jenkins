pipeline {
    agent {
     label 'master'
    }

    stages {
        stage('Create docker image') {
            steps {
                echo "==================== start building image ========================"
                dir('docker'){
                     sh 'docker build . '
                }
            }
        }
            stage('Gradle Test') {
                 steps {
                     sh 'gradle clean test'
            }
        }
    }
}
